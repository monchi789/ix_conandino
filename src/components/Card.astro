---
const { image, description, title, subtitle, sumary } = Astro.props;
---

<div class="flex flex-col md:max-w-[25rem] drop-shadow-2xl">
  <img class="rounded-t-2xl object-cover h-[25rem]" src={image} alt={description}/>
  <div class="flex flex-col gap-y-4 py-4 px-4 rounded-b-2xl bg-[#F9F9F9]">
    <div class="">
      <h3 class="font-semibold text-xl text-[#0075B1]">{title}</h3>
      <h4 class="font-medium text-[#716F76]">{subtitle}</h4>
    </div>
    <p class="font-normal text-justify text-[#716F76]">{sumary}</p>
  </div>
</div>
